# Edge connector (scaffold only)

C#/.NET 8+ site edge connector: access panels, printers, local hardware, durable
encrypted command queue, panel-side credential expiry. Outbound-only mTLS.

Phase 2 — no logic yet. The project skeleton and command-queue implementation
go through the normal `/plan-story` pipeline once that phase starts.

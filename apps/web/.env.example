VITE_API_BASE_URL=http://localhost:8000

# Entra ID (MSAL) staff SSO app registration — placeholders only. There is no
# real Entra app registration available in this environment; replace with
# real values before any non-local deployment. Never a real tenant ID here.
VITE_ENTRA_CLIENT_ID=00000000-0000-0000-0000-000000000000
VITE_ENTRA_TENANT_ID=placeholder-tenant-id
VITE_ENTRA_REDIRECT_URI=http://localhost:5173

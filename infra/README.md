# Infra (placeholder)

Terraform/Bicep for the Azure-first deployment target (PostgreSQL Flexible
Server, Redis, Service Bus + transactional outbox, Blob Storage, Entra ID,
Key Vault, Container Apps/AKS).

Phase 2+ — nothing provisioned yet. Local dev uses `docker-compose.yml` at
the repo root instead.
